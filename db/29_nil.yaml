title: nil literal
title_html: <code>nil</code> literal
level: expressions_and_types
features: run step instructions console
under_construction: false
yellow:
- code: |
    puts nil
    p    nil
    puts ""
    p    ""
    puts "nil"
    p    "nil"
    puts "#{nil}"
    p    "#{nil}"
    
blue:
- code: |
    puts "nil#{nil}"
    p    "nil#{nil}"
    puts "#{nil}" * 2
    p    "#{nil}" * 2

  cases:
  - expected_output: |
      nil
      "nil"
      
      ""
red:
- code: |
    x = "nil"
    p x       #
  cases:
  - expected_output: nil
green:
- code: |
    puts "a#{x}b" #
  cases:
  - expected_output: ab
